<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>LLM | 特倫蘇的日與夜</title><link>https://blog.cklau.cc/tags/llm/</link><atom:link href="https://blog.cklau.cc/tags/llm/index.xml" rel="self" type="application/rss+xml"/><description>LLM</description><generator>Source Themes Academic (https://sourcethemes.com/academic/)</generator><language>en</language><lastBuildDate>Thu, 23 Apr 2026 00:18:00 +0000</lastBuildDate><item><title>🧠 Four Axes of LLM Mechanistic Interpretability</title><link>https://blog.cklau.cc/scientia/llm-mechanistic-interpretability/</link><pubDate>Thu, 23 Apr 2026 00:18:00 +0000</pubDate><author>Junjie LIU</author><category>Artificial Intelligence</category><category>XAI</category><category>interpretability</category><category>mechanistic interpretability</category><category>LLM</category><guid>https://blog.cklau.cc/scientia/llm-mechanistic-interpretability/</guid><description>TL;DR Mechanistic interpretability of transformers has matured along three axes: states (Logit / Tuned Lens, probing), causality (activation patching, ACDC, feature circuits), and contrastive states (RepE, CAA, steering vectors). Most interpretability blogs cover the first three. A fourth lineage exists in the dynamical-systems community, e.g., block-Jacobian spectra, mean-field / Vlasov limits, phase portraits, Langevin / Fokker–Planck fits, Koopman / DMD, and Liang–Kleeman information flow. This lineage studies the residual stream unconditionally, i.</description><content:encoded>&lt;![CDATA[<h2 id="tldr">TL;DR</h2><ul><li>Mechanistic interpretability of transformers has matured along three axes:<strong>states</strong> (Logit / Tuned Lens, probing),<strong>causality</strong> (activation patching, ACDC, feature circuits), and<strong>contrastive states</strong> (RepE, CAA, steering vectors). Most interpretability blogs cover the first three.</li><li>A fourth lineage exists in the dynamical-systems community, e.g., block-Jacobian spectra, mean-field / Vlasov limits, phase portraits, Langevin / Fokker–Planck fits, Koopman / DMD, and Liang–Kleeman information flow. This lineage studies the residual stream<strong>unconditionally</strong>, i.e., as background structure, not under task contrasts.</li><li>The missing fourth axis is<strong>differential transition operators</strong>, i.e., how the layer-$\ell$ update map $f_\ell$ changes between two task conditions. Object from Axis 4. Framing from Axes 2 and 3. Nobody has bridged them.</li><li>There is no unified pathway-share metric, i.e., one number per layer comparable across tasks and scales that says how much work goes through attention vs MLP. Such a metric lives on the transition $f_\ell$, not on the state $h_\ell$.</li><li>Audience: someone who has read the IOI paper and Logit Lens, but not the dynamics literature.</li></ul><h2 id="before">Before</h2><p>Most interpretability blog posts walk through methods in chronological order, e.g., Logit Lens first, then activation patching, then ACDC, then sparse autoencoders, then steering vectors. After reading them you know what each method is called, but you don&rsquo;t really know what question each method is trying to answer. The literature is small enough that the methods recombine, and the chronological reading flattens the actual structure.</p><p>A more useful organization is by<strong>target</strong>, i.e., which aspect of the model does each generation of methods try to explain. Once organized this way, the existing eras line up onto three axes (states, causality, dynamics), and a fourth axis (differential transition operators) is sitting in plain sight. The reason it is sitting in plain sight is that the interpretability crowd and the dynamical-systems crowd do not really read each other&rsquo;s papers.</p><p>This post is for someone who has read the IOI paper [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-1" title="K. Wang, A. Variengien, A. Conmy, B. Shlegeris, and J. Steinhardt, Interpretability in the wild: A circuit for indirect object identification in GPT-2 small, In Proc. International Conference on Learning Representations, 2023">1</a>] and skimmed Logit Lens, but has never heard of Koopman operators or Fokker–Planck. The first three axes are a fast recap. The fourth one is the dynamics literature, plus the gap that I want to highlight.</p><h2 id="states">States</h2><p>The oldest interpretability tradition for transformers is<strong>state readout</strong>. Take the hidden activation at intermediate layer $\ell$, project it back into vocabulary space, and read off what the model &ldquo;thinks&rdquo; it is going to say at that depth. nostalgebraist&rsquo;s<a href="https://www.lesswrong.com/posts/AcKRB8wDpdaN6v6ru/interpreting-gpt-the-logit-lens">Logit Lens post on LessWrong (2020)</a> is the canonical version: apply the unembedding matrix $W_U$ directly to the residual stream $h_\ell$, and look at the top-k tokens of $\text{softmax}(W_U h_\ell)$. The result is informative: by mid-depth the residual stream&rsquo;s top tokens already concentrate on plausible continuations.</p><p>The problem is that Logit Lens implicitly assumes the basis at every layer is the same basis the unembedding was trained on, which is only approximately true.<strong>Tuned Lens</strong> [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-2" title="N. Belrose, Z. Furman, L. Smith, D. Halawi, I. Ostrovsky, L. McKinney, S. Biderman, and J. Steinhardt, Eliciting latent predictions from transformers with the tuned lens, arXiv preprint arXiv:2303.08112, 2023. ">2</a>] fixes this with a small affine probe $A_\ell$ trained per layer, such that $\text{softmax}(W_U A_\ell h_\ell)$ matches the final-layer distribution. The lens becomes calibrated rather than naive.</p><p><img src="/images/scientia/mi-logit-vs-tuned-lens.svg" alt="Fig. 1 — Logit Lens vs Tuned Lens."> Linear probing is the same family of idea pushed further: train a small classifier on top of $h_\ell$ to recover any property you care about, e.g., sentiment, syntax, factual recall, position, or the answer to a multiple-choice question, and ask at which depth the property becomes linearly decodable.</p><p>These methods answer one question well: what does the model represent, and where in the stack? They do not answer a second question: how is that representation built? A linear probe at layer 12 tells you that subject information is there. It does not tell you which earlier layers wrote it, or how stable the encoding is.</p><h2 id="causality">Causality</h2><p>The intervention era is the response to the gap above. The unifying move is<strong>counterfactual ablation</strong>: run the model on a clean prompt $x$, run it again on a corrupted prompt $x&rsquo;$ that differs in one feature, then patch some component (e.g., a head&rsquo;s output, an MLP block, a single residual position) from one run into the other and see if the prediction follows the patch. If patching head $\ell.h$ from the clean run into the corrupted run recovers the clean prediction, that head is causally responsible for the behavior.</p><p>Activation patching gives per-component effect sizes.<strong>Attribution patching</strong> linearizes the patch with a first-order gradient approximation, such that you do not need to rerun the model once per intervention.<strong>ACDC</strong> (Automated Circuit DisCovery) [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-3" title="A. Conmy, A. Mavor-Parker, A. Lynch, S. Heimersheim, and A. Garriga-Alonso, Towards automated circuit discovery for mechanistic interpretability, In Proc. Advances in Neural Information Processing Systems, 2023">3</a>] automates the next step: greedily prune the model&rsquo;s computation graph until only the components whose removal hurts the metric remain, and report the resulting sparse subgraph as a &ldquo;circuit&rdquo;. Feature circuits push the same idea down to SAE features rather than whole heads or MLPs.</p><p>The intervention era is what lets us make claims like &ldquo;this head copies the indirect object&rdquo; or &ldquo;this MLP stores the capital-of-country fact&rdquo;. It is the most causally honest axis we have. The cost is twofold: (a) every claim is per-task, i.e., one circuit per behavior, and the search is expensive per hypothesis; and (b) the output is a graph of components, not a description of the transition connecting them. Patching tells you head 9.6 matters for IOI. It does not give you an operator that maps &ldquo;subject is in residual stream&rdquo; to &ldquo;indirect object will be next token&rdquo;.</p><h2 id="contrastive-states">Contrastive States</h2><p>The third axis takes a more pragmatic stance. Instead of asking which components compute a behavior, it asks: does the activation difference between behaviors live in a low-dimensional subspace, and can we move along that subspace to control the behavior?</p><p>The recipe is the same across this whole literature. Collect activations on contrastive prompt pairs, e.g., &ldquo;happy&rdquo; vs &ldquo;sad&rdquo;, &ldquo;truthful&rdquo; vs &ldquo;deceptive&rdquo;, or &ldquo;refuses&rdquo; vs &ldquo;complies&rdquo;. Take the mean activation difference $\Delta h_\ell = \mathbb{E}[h_\ell \mid +] - \mathbb{E}[h_\ell \mid -]$. Use that vector as both an analysis object (a direction in activation space tagged with semantic meaning) and a manipulation handle (add or subtract a scaled copy at inference time, and watch the behavior change).<strong>Representation Engineering</strong> [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-4" title="A. Zou, L. Phan, S. Chen, J. Campbell, P. Guo, R. Ren, A. Pan, X. Yin, M. Mazeika, A. Dombrowski, and others, Representation engineering: A top-down approach to AI transparency, arXiv preprint arXiv:2310.01405, 2023. ">4</a>]<span class="sidenote-number"><small class="sidenote">
A bit of a name collision with "representation learning". They are not the same thing, despite the vocabulary overlap.</small></span> formalizes this for high-level concepts like honesty and emotion.<strong>Contrastive Activation Addition (CAA)</strong> is the same idea localized to a single layer. The broader<strong>steering-vector</strong> literature treats it as a general control surface for safety-relevant behaviors.</p><p>This axis is appealing because it links state geometry to behavior with one cheap forward pass. The limitation is built in: it operates at the state level, not at the transition level. A steering vector tells you which direction in $\mathbb{R}^{d}$ correlates with the contrast. It does not tell you how the update map differs between the two conditions. If two behaviors share the same mean shift but differ in how the residual stream evolves around that shift, e.g., different curvature, different mixing rates, or different attention routing, the steering vector cannot see it.</p><p>So Axes 1 and 3 both target<strong>state</strong>. Axis 1 asks what is decodable from the state. Axis 3 asks what direction in state-space shifts behavior. Axis 2 is the<strong>causality</strong> axis. None of the three speak to how the state moves.</p><h2 id="dynamics-an-unread-lineage">Dynamics: An Unread Lineage</h2><p>The residual stream is a trajectory:</p><p>$$h_{0} \to h_{1} \to \dots \to h_{L}, \quad h_{\ell+1} = h_{\ell} + f_{\ell}(h_{\ell}),$$</p><p>where $f_{\ell}$ is whatever the $\ell$-th block computes (attention + MLP, with a residual bypass). This is a discrete-time dynamical system, and dynamical-systems people have a lot to say about discrete-time dynamical systems.</p><p><img src="/images/scientia/mi-residual-trajectory.svg" alt="Fig. 2 — Residual-stream trajectory and Fokker–Planck potential landscape."> Five threads worth knowing about:</p><ul><li><p><strong>Block-Jacobian spectra.</strong> Li and Papyan [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-5" title="J. Li and V. Papyan, Residual alignment: uncovering the mechanisms of residual networks, In Proc. Advances in Neural Information Processing Systems, 2023, vol. 36, pp. 57660–57712. ">5</a>] compute the Jacobian of one transformer block, $J_\ell = \partial h_{\ell+1}/\partial h_\ell$, and characterize its eigenvalue distribution across depth. They call the phenomenon<em>residual alignment</em>, i.e., the residual updates align with a shrinking set of singular directions as depth grows. Early layers behave like contraction, mid layers expand a few subspaces, late layers re-collapse onto a low-dimensional output manifold. This gives a depth-dependent operator picture rather than a state picture.</p></li><li><p><strong>Mean-field / Vlasov limits.</strong> Castin et al. take the infinite-token limit of self-attention and derive a continuity equation for the empirical distribution of token representations, such that the residual stream becomes a flow on a measure space. The transformer becomes a discretization of a particular partial differential equation. This is the cleanest theoretical entry point for asking &ldquo;what does attention do to a distribution of tokens?&rdquo;</p></li><li><p><strong>Phase portraits.</strong> Fernando and Guitchounts visualize forward dynamics as trajectories in low-dimensional projections of the residual stream, and look for fixed points, limit cycles, and saddle structure. The picture that emerges is depth as a slow flow toward attractor manifolds, which is qualitatively different from the &ldquo;the model thinks at layer 12&rdquo; mental image you get from probing.</p></li><li><p><strong>Stochastic / Fokker–Planck fits.</strong> Sarfati et al. fit a Langevin-type model
$$dh = -\nabla U(h),d\ell + \sigma,dW_\ell$$
to the residual-stream evolution, and recover an effective potential $U(h)$ whose minima correspond to high-confidence answers. The picture is: forward depth is<strong>integration time</strong>, i.e., each layer is one step of an SDE rather than an arbitrary computation; the residual stream<strong>flows downhill on a potential landscape</strong> $U(h)$; and the equilibrium distribution at any depth has the Boltzmann form $p(h) \propto e^{-U(h)/T}$, such that the model&rsquo;s softmax confidence corresponds to a temperature on the basins of $U$. The dual view is the Fokker–Planck equation
$$\partial_\ell,p(h, \ell) = \nabla \cdot \bigl(p,\nabla U\bigr) + \tfrac{\sigma^{2}}{2}\nabla^{2} p,$$
which is the distributional view that Vlasov / mean-field analyses operate in. This is the only thread in the lineage that explicitly treats depth as integration time.</p></li><li><p><strong>Operator-theoretic methods (Koopman, DMD).</strong> Approaches in the spirit of ATO apply Dynamic Mode Decomposition to the layer-by-layer activations to extract a linear operator that best explains the (nonlinear) state evolution. The Koopman framing converts a nonlinear dynamical question into a linear spectral one, i.e., modes, frequencies, decay rates.</p></li><li><p><strong>Information-theoretic flow (Liang–Kleeman).</strong> The Liang–Kleeman framework quantifies the causal influence of one variable on another in a dynamical system, with units of bits per time step, and importantly does not conflate correlation with causation. Applied to the residual stream, it would say not just &ldquo;head 9.6 matters&rdquo; but &ldquo;head 9.6 transmits $X$ bits per layer to position $i$ about feature $Y$&rdquo;. Almost nobody in the interpretability literature has done this at scale.</p></li></ul><p>The important caveat is that this entire lineage studies<strong>unconditional</strong> dynamics. It characterizes the flow on average, in expectation over the data distribution, or as a property of the trained network independent of any task. It is background structure, not task-specific structure. The Jacobian spectrum of layer 9 is a property of the network. The IOI circuit is a property of the network under a particular task contrast. These are different objects, and so far they live in different papers, written by different communities.</p><h2 id="the-gap">The Gap</h2><p><img src="/images/scientia/mi-four-axes-quadrant.svg" alt="Fig. 3 — The four eras arranged by (object of study × task conditioning); the bottom-right cell is the gap."/><p>Put the four eras side by side:</p><table><thead><tr><th>Axis</th><th>Object of study</th><th>Granularity</th><th>Conditional on task?</th></tr></thead><tbody><tr><td>1. States (Logit / Tuned Lens, probes)</td><td>$h_\ell$</td><td>per layer</td><td>partly, probes are</td></tr><tr><td>2. Causality (patching, ACDC, feature circuits)</td><td>components</td><td>per head/MLP</td><td>yes, one circuit per task</td></tr><tr><td>3. Contrastive states (RepE, CAA, steering)</td><td>$\Delta h_\ell$</td><td>per layer/region</td><td>yes</td></tr><tr><td>4. Dynamics (Jacobians, Koopman, Vlasov, L-K)</td><td>transition $f_\ell$</td><td>per layer</td><td><strong>no, unconditional</strong></td></tr></tbody></table><p>The diagonal entry is empty. There is no body of work that asks &ldquo;how does the transition operator $f_\ell$ change between two task conditions?&rdquo; Axis 4 has the right object (the transition). Axes 2 and 3 have the right framing (contrastive, conditional). Nobody has explicitly put them together.</p><p>This combination, i.e.,<strong>differential transition operators</strong>, would let you say things like:</p><blockquote><p>&ldquo;Under the IOI task vs. its negative control, the layer-9 transition operator gains a rank-2 component that routes from position 7 to the residual at position 11, with information flow of $X$ bits per layer.&rdquo;</p></blockquote><p>This is a statement about how the computation differs between two conditions, not about which components fire (Axis 2), what direction the state shifts (Axis 3), or how the network flows on average (Axis 4). It is a fourth axis.</p><p>The toolbox already supports it. Block-Jacobian spectra trivially differ across conditions, you just have to compute them under a contrast pair. Koopman / DMD on residual streams admits a natural decomposition into condition-specific modes. Liang–Kleeman is defined contrastively. The ingredients are sitting on the shelf.</p><h2 id="attention-vs-mlp-and-scale">Attention vs MLP, and Scale</h2><p>A natural objection: &ldquo;but the IOI circuit is almost entirely attention heads, so why bother with transition operators when components already explain the behavior?&rdquo;</p><p>IOI is GPT-2 small, i.e., 117M parameters and 12 layers, with a task picked specifically because it has a clean attention-routing structure. The folklore that</p><blockquote><p>attention = routing/copying/induction, and MLP = key–value memory</p></blockquote><p>is a useful heuristic at that scale, but it starts to break down before 1B parameters. Gated-FFN and MoE architectures push MLPs into routing-like behavior. Attention picks up more pure memorization, especially in long-context retrieval. The cleanly separable &ldquo;pathway-share&rdquo; you can compute on GPT-2 small becomes much fuzzier as the model grows.</p><p>What we do not have today is a unified pathway-share metric, i.e., one number per layer comparable across tasks and across model scales that says &ldquo;of the work done at this layer, $\alpha$ went through attention and $1-\alpha$ through the MLP&rdquo;. Such a metric is naturally a<strong>transition</strong> quantity, not a<strong>state</strong> quantity. It lives on $f_\ell$, not on $h_\ell$. The missing fourth axis is missing for an operational reason, not just a taxonomical one.</p><h2 id="closing">Closing</h2><p>If you have spent your time reading the canonical interpretability blogs (Logit Lens, IOI, ACDC, the SAE papers, the steering-vector posts), your mental model of the field is probably organized by method. A more useful organization is by what the method tries to explain: states, causality, contrastive states, or transitions. The first three are mature. The fourth (dynamics) has a deep, careful, mostly-unread literature attached to it, but that literature studies the network unconditionally, which is exactly the thing interpretability does not need.</p><p>The next interpretability tool worth building, in my opinion, is a Koopman-style (or Jacobian-spectrum, or Liang–Kleeman) analysis run<strong>contrastively</strong> across two task conditions, and reported as a per-layer, per-pathway differential operator. The math is on the shelf.</p><h2 id="further-reading-community-posts">Further Reading (Community Posts)</h2><p>A lot of useful mechanistic-interpretability material lives outside conventional venues, e.g., LessWrong, the Alignment Forum, Anthropic&rsquo;s transformer-circuits.pub, and Neel Nanda&rsquo;s blog. Some posts I keep coming back to:</p><ul><li><strong>nostalgebraist (2020),<a href="https://www.lesswrong.com/posts/AcKRB8wDpdaN6v6ru/interpreting-gpt-the-logit-lens"><em>Interpreting GPT: the Logit Lens</em></a>.</strong> The original Logit Lens post. Read this first if you have not. Most of Axis 1 is a refinement of the empirical observation made here.</li><li><strong><a href="https://www.lesswrong.com/posts/ATsvzF77ZsfWzyTak/dataset-sensitivity-in-feature-matching-and-a-hypothesis-on-1"><em>SAE Training Dataset Influence in Feature Matching and a Hypothesis on Position Features</em></a>.</strong> A reminder that which dataset you train your SAE on changes which features it discovers, i.e., SAE features are not a property of the model alone, but of the (model, dataset) pair. Worth reading because even &ldquo;model-internal&rdquo; objects are silently conditioned on data, which is the contrast / conditioning move I am pushing for in Axis 4.</li><li><strong>Elhage et al.,<a href="https://transformer-circuits.pub/2021/framework/index.html"><em>A Mathematical Framework for Transformer Circuits</em></a> (Anthropic, 2021).</strong> The bilinear / rank decomposition that makes the QK-OV picture of attention legible. Most subsequent circuit papers borrow this scaffolding.</li><li><strong>Olsson et al.,<a href="https://transformer-circuits.pub/2022/in-context-learning-and-induction-heads/index.html"><em>In-context Learning and Induction Heads</em></a> (Anthropic, 2022).</strong> The induction-head story. A worked example of &ldquo;we found the mechanism&rdquo; at small scale, and a useful baseline for asking when that picture stops working at larger scale.</li><li><strong>Neel Nanda,<a href="https://www.neelnanda.io/mechanistic-interpretability/glossary"><em>A Comprehensive Mechanistic Interpretability Explainer &amp; Glossary</em></a>.</strong> Probably the best entry point if you are landing in this field cold.</li><li><strong>Neel Nanda,<a href="https://www.lesswrong.com/posts/jP9KDyMkchuv6tHwm/how-to-become-a-mechanistic-interpretability-researcher"><em>How To Become A Mechanistic Interpretability Researcher</em></a> (2025).</strong> A staged curriculum and career guide rather than a technical post. If you are reading this blog because you want to<em>do</em> mech-interp and not just understand the literature, this is the practical companion piece, i.e., what to learn, in what order, how to find mentorship, how to ship work.</li></ul><p>If you only have time for one of these, read nostalgebraist&rsquo;s Logit Lens post. Most of Axis 1 is downstream of it.</p><h2 id="reference">Reference</h2><ol class="hugo-simplecite-reference-list"><li class="hugo-simplecite-reference-list-item" id="bibreference-1">K. Wang, A. Variengien, A. Conmy, B. Shlegeris, and J. Steinhardt,<q>Interpretability in the wild: A circuit for indirect object identification in GPT-2 small,</q> In Proc. International Conference on Learning Representations, 2023</li><li class="hugo-simplecite-reference-list-item" id="bibreference-2">N. Belrose, Z. Furman, L. Smith, D. Halawi, I. Ostrovsky, L. McKinney, S. Biderman, and J. Steinhardt,<q>Eliciting latent predictions from transformers with the tuned lens,</q><em>arXiv preprint arXiv:2303.08112</em>, 2023.</li><li class="hugo-simplecite-reference-list-item" id="bibreference-3">A. Conmy, A. Mavor-Parker, A. Lynch, S. Heimersheim, and A. Garriga-Alonso,<q>Towards automated circuit discovery for mechanistic interpretability,</q> In Proc. Advances in Neural Information Processing Systems, 2023</li><li class="hugo-simplecite-reference-list-item" id="bibreference-4">A. Zou, L. Phan, S. Chen, J. Campbell, P. Guo, R. Ren, A. Pan, X. Yin, M. Mazeika, A. Dombrowski, and others,<q>Representation engineering: A top-down approach to AI transparency,</q><em>arXiv preprint arXiv:2310.01405</em>, 2023.</li><li class="hugo-simplecite-reference-list-item" id="bibreference-5">J. Li and V. Papyan,<q>Residual alignment: uncovering the mechanisms of residual networks,</q> In Proc. Advances in Neural Information Processing Systems, 2023, vol. 36, pp. 57660–57712.</li></ol>
]]></content:encoded></item><item><title>🧑🏿‍💻 Train A Language Model From Scratch</title><link>https://blog.cklau.cc/scientia/llm-from-scratch/</link><pubDate>Fri, 27 Dec 2024 00:15:00 +0000</pubDate><author>Junjie LIU</author><category>Artificial Intelligence</category><category>language model</category><category>LLM</category><guid>https://blog.cklau.cc/scientia/llm-from-scratch/</guid><description>Beginning You may think that the training a language model from scratch can seem like a daunting task, but it is a rewarding journey that deepens your understanding of machine learning and natural language processing. In this guide, we will explore the fundamental steps required to build a language model, from data preparation to model training and evaluation. Whether you&amp;rsquo;re a beginner or an experienced practitioner, this process will provide valuable insights into the inner workings of large language models.</description><content:encoded>&lt;![CDATA[<h2 id="beginning">Beginning</h2><p>You may think that the training a language model from scratch can seem like a daunting task, but it is a rewarding journey that deepens your understanding of machine learning and natural language processing. In this guide, we will explore the fundamental steps required to build a language model, from data preparation to model training and evaluation. Whether you&rsquo;re a beginner or an experienced practitioner, this process will provide valuable insights into the inner workings of large language models.</p><h2 id="starting-from-the-tasks">Starting from the tasks</h2><p>When training a language model, various tasks are employed to help the model learn and generalize effectively. These tasks are tailored to different stages of the training process, such as pretraining, supervised fine-tuning (SFT), and reinforcement learning with human feedback (RLHF). Below are some of the most widely used tasks, along with detailed explanations:</p><ul><li><p><strong>Masked Language Modeling (MLM):</strong><br>
Starting from the MLM, which is widely used in the word embedding training, proposed by Google in 2017 [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-1" title="J. Devlin, M. Chang, K. Lee, and K. Toutanova, Bert: Pre-training of deep bidirectional transformers for language understanding, In Proc. Proceedings of the 2019 conference of the north american chapter of the association for computational linguistics: Human language technologies, volume 1 (long and short papers), 2019, pp. 4171–4186. ">1</a>]. In short, it is a pretraining task where certain tokens in the input sequence are randomly masked, and the model is trained to predict these masked tokens based on their surrounding context. This task enables the model to learn bidirectional representations of text, making it effective for understanding the relationships between words in a sentence. For example, in the sentence &ldquo;The [MASK] is blue,&rdquo; the model learns to predict the masked word &ldquo;sky.&rdquo; Models like BERT and RoBERTa heavily rely on MLM for pretraining. The<code>DataCollatorForLanguageModeling</code> in the Hugging Face Transformers library is often used to implement this task, where it handles token masking dynamically during training.</p></li><li><p><strong>Causal Language Modeling (CLM):</strong><br>
CLM, also known as autoregressive modeling, involves predicting the next token in a sequence based on the previous tokens. This task is unidirectional, meaning the model only considers past context when generating the next word. For instance, given the input &ldquo;The cat is,&rdquo; the model predicts the next word, such as &ldquo;sleeping.&rdquo; CLM is the backbone of models like GPT and GPT-2, which are designed for text generation tasks. The<code>DataCollatorForLanguageModeling</code> can be used for CLM, depending on the implementation.</p></li><li><p><strong>Supervised Fine-Tuning (SFT):</strong><br>
SFT is a critical step after pretraining, where the model is fine-tuned on labeled datasets for specific downstream tasks. These tasks can include text classification, summarization, or question answering. For example, in summarization, the model is trained to generate concise summaries of input documents. During SFT, the model learns to adapt its pretrained knowledge to domain-specific requirements. The<code>DataCollatorForSeq2Seq</code> is often used for tasks like summarization, as it handles input-output pairs efficiently.</p></li><li><p><strong>Reinforcement Learning with Human Feedback (RLHF):</strong><br>
RLHF is used to align the model&rsquo;s outputs with human preferences and ethical considerations. This process involves three main steps:</p><ol><li><strong>Supervised Fine-Tuning:</strong> A base model is fine-tuned on a dataset of human-labeled examples.</li><li><strong>Reward Model Training:</strong> A separate reward model is trained to score the quality of the model&rsquo;s outputs based on human feedback.</li><li><strong>Policy Optimization:</strong> The language model is further fine-tuned using reinforcement learning, guided by the reward model.<br>
RLHF is particularly useful for tasks like conversational AI, where the model needs to generate responses that are both helpful and aligned with user expectations.</li></ol></li></ul><p>By combining these tasks, we can train language models that are not only powerful but also versatile and aligned with human expectations. Each task plays a unique role in shaping the model&rsquo;s capabilities, from understanding context to generating coherent and meaningful text.</p><h2 id="what-about-the-data-format">What about the Data Format</h2><p>The format of the training data plays a crucial role in determining the effectiveness of the language model. Different formats are used depending on the task and the stage of training, such as pretraining, fine-tuning, or reinforcement learning. Below, we discuss some commonly used data formats, including the Alpaca format and Vicuna&rsquo;s multi-round chat format.</p><h3 id="alpaca-format">Alpaca Format</h3><p>The Alpaca format is designed for instruction-tuning tasks, where the model is fine-tuned to follow instructions and generate helpful responses. This format typically consists of a JSON structure with fields such as<code>instruction</code>,<code>input</code>, and<code>output</code>. Here&rsquo;s an example:</p><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{</span></span><span style="display:flex;"><span><span style="color:#f92672">"instruction"</span>:<span style="color:#e6db74">"Summarize the following text."</span>,</span></span><span style="display:flex;"><span><span style="color:#f92672">"input"</span>:<span style="color:#e6db74">"Artificial intelligence is a branch of computer science that aims to create machines that can perform tasks that typically require human intelligence."</span>,</span></span><span style="display:flex;"><span><span style="color:#f92672">"output"</span>:<span style="color:#e6db74">"Artificial intelligence is a field focused on creating machines capable of human-like intelligence."</span></span></span><span style="display:flex;"><span>}</span></span></code></pre></div><ul><li><strong>Instruction:</strong> Specifies the task the model should perform.</li><li><strong>Input:</strong> Provides additional context or data required to complete the task. This field can be empty for tasks that don&rsquo;t require extra input.</li><li><strong>Output:</strong> Contains the expected response or result for the given instruction and input.</li></ul><p>This format is widely used in fine-tuning models like Alpaca and other instruction-following models. It ensures that the model learns to handle a variety of tasks in a structured manner.</p><h3 id="vicunas-multi-round-chat-format">Vicuna&rsquo;s Multi-Round Chat Format</h3><p>Vicuna&rsquo;s multi-round chat format is tailored for conversational AI models, where the goal is to train the model to handle multi-turn dialogues effectively. This format captures the back-and-forth nature of human conversations. An example of this format is as follows:</p><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{</span></span><span style="display:flex;"><span><span style="color:#f92672">"conversation"</span>: [</span></span><span style="display:flex;"><span> {<span style="color:#f92672">"role"</span>:<span style="color:#e6db74">"user"</span>,<span style="color:#f92672">"content"</span>:<span style="color:#e6db74">"What is the capital of France?"</span>},</span></span><span style="display:flex;"><span> {<span style="color:#f92672">"role"</span>:<span style="color:#e6db74">"assistant"</span>,<span style="color:#f92672">"content"</span>:<span style="color:#e6db74">"The capital of France is Paris."</span>},</span></span><span style="display:flex;"><span> {<span style="color:#f92672">"role"</span>:<span style="color:#e6db74">"user"</span>,<span style="color:#f92672">"content"</span>:<span style="color:#e6db74">"Can you tell me more about it?"</span>},</span></span><span style="display:flex;"><span> {<span style="color:#f92672">"role"</span>:<span style="color:#e6db74">"assistant"</span>,<span style="color:#f92672">"content"</span>:<span style="color:#e6db74">"Paris is known as the City of Light and is famous for its art, culture, and landmarks like the Eiffel Tower."</span>}</span></span><span style="display:flex;"><span> ]</span></span><span style="display:flex;"><span>}</span></span></code></pre></div><ul><li><strong>Role:</strong> Indicates whether the message is from the<code>user</code> or the<code>assistant</code>.</li><li><strong>Content:</strong> Contains the text of the message.</li></ul><p>This format is particularly useful for fine-tuning models to generate coherent and contextually relevant responses in multi-turn conversations. It helps the model maintain context across multiple exchanges, making it suitable for chatbots and virtual assistants. However, I don&rsquo;t think the format are most important</p><p>However, different formats can be leveraged by the<code>process</code> function, which means the format is not the first priority of in the training process, instead, the data itself is. Below is a example for converting vicuna format to alpaca.</p><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span><span style="color:#a6e22e">vicuna_to_alpaca</span>(conversation):</span></span><span style="display:flex;"><span> user<span style="color:#f92672">=</span> [conv<span style="color:#f92672">.</span>get(<span style="color:#e6db74">"user"</span>,<span style="color:#e6db74">"None"</span>)<span style="color:#66d9ef">for</span> conv<span style="color:#f92672">in</span> conversation]</span></span><span style="display:flex;"><span> assistant<span style="color:#f92672">=</span> [conv<span style="color:#f92672">.</span>get(<span style="color:#e6db74">"assistant"</span>,<span style="color:#e6db74">"None"</span>)<span style="color:#66d9ef">for</span> conv<span style="color:#f92672">in</span> conversation]</span></span><span style="display:flex;"><span> instruct<span style="color:#f92672">=</span> [conv<span style="color:#f92672">.</span>get(<span style="color:#e6db74">"instruction"</span>,<span style="color:#e6db74">"None"</span>)<span style="color:#66d9ef">for</span> conv<span style="color:#f92672">in</span> conversation]</span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span> instruct, user, assistant</span></span></code></pre></div><h2 id="data-filtering">Data Filtering</h2><p>Although format might be important, data quality is much more vital for the training. As the raw corpora are riddled with inconsistencies, artifacts, and ethical pitfalls. Deduplication, for instance, goes beyond simple substring matching: advanced methods like MinHash or SimHash identify near-duplicates at scale, while fuzzy hashing detects paraphrased or lightly edited repetitions. Similarly, normalization involves granular steps: Unicode normalization (e.g., converting “café” and “café” to a consistent form), stripping non-linguistic markup (e.g., LaTeX, HTML tags), and handling whitespace irregularities (e.g., replacing multiple spaces or tabs with a single space). Tools like ftfy (“fixes text for you”) repair mojibake (garbled text from encoding errors). What&rsquo;s more, corpora can also contain toxicity and bias, and filtering these harmful contents requires multi-layered strategies. Model-based methods are now most widely used — like Google&rsquo;s Perspective API or HuggingFace&rsquo;s detoxify flag can help remove explicit hate speech, but nuanced toxicity (e.g., microaggressions, sarcasm) demands custom fine-tuning.</p><p>Different stages have different filtering priorities. For pretraining the goal is<em>coverage and quality at scale</em>, so most pipelines combine cheap rule-based filters (length thresholds, language ID, gibberish ratio, repetition ratio) with one or two model-based passes (perplexity scoring against a small reference LM, classifier-based quality scoring like the fastText classifier used in CCNet/RedPajama). For instruction-tuning the goal flips: the dataset is small enough that<em>each example matters</em>, so the filter cares about response correctness, helpfulness, and stylistic consistency — typically scored by a stronger LLM acting as judge. For preference data (used in RLHF/DPO), the priority is<em>agreement among annotators</em> and the<em>margin</em> between chosen and rejected responses; low-margin pairs are noisy and hurt more than they help.</p><p>A practical heuristic I keep coming back to: if you can describe the filter in one sentence, you should run it before training; if it takes a paragraph, it probably belongs in your eval suite instead.</p><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># A minimal pretraining filter chain that covers most of the obvious cases.</span></span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> re<span style="color:#f92672">,</span> ftfy</span></span><span style="display:flex;"><span/></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span><span style="color:#a6e22e">basic_clean</span>(text: str)<span style="color:#f92672">-></span> str<span style="color:#f92672">|</span><span style="color:#66d9ef">None</span>:</span></span><span style="display:flex;"><span> text<span style="color:#f92672">=</span> ftfy<span style="color:#f92672">.</span>fix_text(text)</span></span><span style="display:flex;"><span> text<span style="color:#f92672">=</span> re<span style="color:#f92672">.</span>sub(<span style="color:#e6db74">r</span><span style="color:#e6db74">"\s+"</span>,<span style="color:#e6db74">" "</span>, text)<span style="color:#f92672">.</span>strip()</span></span><span style="display:flex;"><span><span style="color:#66d9ef">if</span> len(text)<span style="color:#f92672">&lt;</span><span style="color:#ae81ff">200</span>:<span style="color:#75715e"># too short to learn anything</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span><span style="color:#66d9ef">None</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">if</span> len(set(text<span style="color:#f92672">.</span>split()))<span style="color:#f92672">/</span> max(<span style="color:#ae81ff">1</span>, len(text<span style="color:#f92672">.</span>split()))<span style="color:#f92672">&lt;</span><span style="color:#ae81ff">0.3</span>:</span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span><span style="color:#66d9ef">None</span><span style="color:#75715e"># extremely repetitive</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">if</span> sum(c<span style="color:#f92672">.</span>isalpha()<span style="color:#66d9ef">for</span> c<span style="color:#f92672">in</span> text)<span style="color:#f92672">/</span> len(text)<span style="color:#f92672">&lt;</span><span style="color:#ae81ff">0.6</span>:</span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span><span style="color:#66d9ef">None</span><span style="color:#75715e"># mostly punctuation/digits</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span> text</span></span></code></pre></div><p>Finally,<em>domain mixing</em>. A pretraining corpus is a weighted mixture: web crawl, code, books, papers, encyclopedias, multilingual text. The mixing weights matter at least as much as the raw data — LLaMA-2 [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-2" title="H. Touvron, L. Martin, K. Stone, and others, Llama 2: Open foundation and fine-tuned chat models, arXiv preprint arXiv:2307.09288, 2023. ">2</a>] and many open replications spend significant effort here, often with separate sampling temperatures per source so that small but high-quality shards (e.g., Wikipedia, arXiv) are upsampled relative to their natural frequency.</p><h2 id="tokenization">Tokenization</h2><p>Once the data is clean, the next decision is<em>how to chop it into tokens</em>. The dominant choice today is<strong>subword tokenization</strong> — a vocabulary of ~32k–128k pieces, learned to balance compression (fewer tokens per sentence) against generalization (rare words still decompose into known pieces).</p><p>Two algorithms dominate:</p><ul><li><strong>Byte-Pair Encoding (BPE)</strong> [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-3" title="R. Sennrich, B. Haddow, and A. Birch, Neural machine translation of rare words with subword units, In Proc. Proceedings of the 54th Annual Meeting of the Association for Computational Linguistics, 2016, pp. 1715–1725. ">3</a>]: start from a character (or byte) vocabulary, then iteratively merge the most frequent adjacent pair until the vocabulary reaches the target size. GPT-2/3/4 and most open models use a byte-level variant, which guarantees the tokenizer never crashes on unseen Unicode.</li><li><strong>Unigram / SentencePiece</strong>: train a probabilistic language model over candidate subwords and prune to a fixed vocabulary by EM. Used by T5, ALBERT, and most multilingual models because it handles whitespace-free scripts (Chinese, Japanese, Thai) more naturally.</li></ul><p>Two practical points that bite people:</p><ol><li><strong>Train the tokenizer on the same distribution as the pretraining corpus.</strong> A tokenizer trained mostly on English will fragment Chinese or code into far more tokens than necessary, which directly inflates training cost and hurts long-context behavior.</li><li><strong>Add special tokens<em>before</em> training, not after.</strong><code>&lt;|im_start|></code>,<code>&lt;|endoftext|></code>,<code>&lt;pad></code>, tool-call delimiters — if you bolt them on after pretraining, their embeddings start from random and the model needs many tokens of fine-tuning before it stops emitting them in weird places.</li></ol><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> tokenizers<span style="color:#f92672">import</span> Tokenizer, models, trainers, pre_tokenizers, decoders</span></span><span style="display:flex;"><span/></span><span style="display:flex;"><span>tok<span style="color:#f92672">=</span> Tokenizer(models<span style="color:#f92672">.</span>BPE())</span></span><span style="display:flex;"><span>tok<span style="color:#f92672">.</span>pre_tokenizer<span style="color:#f92672">=</span> pre_tokenizers<span style="color:#f92672">.</span>ByteLevel(add_prefix_space<span style="color:#f92672">=</span><span style="color:#66d9ef">False</span>)</span></span><span style="display:flex;"><span>tok<span style="color:#f92672">.</span>decoder<span style="color:#f92672">=</span> decoders<span style="color:#f92672">.</span>ByteLevel()</span></span><span style="display:flex;"><span>trainer<span style="color:#f92672">=</span> trainers<span style="color:#f92672">.</span>BpeTrainer(</span></span><span style="display:flex;"><span> vocab_size<span style="color:#f92672">=</span><span style="color:#ae81ff">64_000</span>,</span></span><span style="display:flex;"><span> special_tokens<span style="color:#f92672">=</span>[<span style="color:#e6db74">"&lt;|endoftext|>"</span>,<span style="color:#e6db74">"&lt;|im_start|>"</span>,<span style="color:#e6db74">"&lt;|im_end|>"</span>,<span style="color:#e6db74">"&lt;pad>"</span>],</span></span><span style="display:flex;"><span> initial_alphabet<span style="color:#f92672">=</span>pre_tokenizers<span style="color:#f92672">.</span>ByteLevel<span style="color:#f92672">.</span>alphabet(),</span></span><span style="display:flex;"><span>)</span></span><span style="display:flex;"><span>tok<span style="color:#f92672">.</span>train(files<span style="color:#f92672">=</span>[<span style="color:#e6db74">"corpus.txt"</span>], trainer<span style="color:#f92672">=</span>trainer)</span></span><span style="display:flex;"><span>tok<span style="color:#f92672">.</span>save(<span style="color:#e6db74">"tokenizer.json"</span>)</span></span></code></pre></div><h2 id="model-architecture">Model Architecture</h2><p>Almost every modern LLM is a decoder-only Transformer [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-4" title="A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. Gomez, Ł. Kaiser, and I. Polosukhin, Attention is all you need, In Proc. Advances in Neural Information Processing Systems, 2017">4</a>] with a stack of identical blocks. The original &ldquo;Attention Is All You Need&rdquo; recipe — multi-head attention, LayerNorm, GELU, learned positional embeddings — has been quietly replaced piece by piece. The current &ldquo;default&rdquo; block, popularized by LLaMA, looks like:</p><ul><li><strong>Pre-norm</strong> instead of post-norm — apply normalization<em>before</em> attention/FFN, which makes training much more stable at depth.</li><li><strong>RMSNorm</strong> instead of LayerNorm — drop the mean-subtraction term; cheaper, and empirically just as good.</li><li><strong>Rotary Positional Embeddings (RoPE)</strong> instead of learned/sinusoidal positions — encode position by rotating the query/key vectors, which extrapolates better to longer contexts.</li><li><strong>SwiGLU</strong> instead of GELU in the feed-forward — gated linear unit with SiLU activation, consistently a small but real win.</li><li><strong>Grouped-Query Attention (GQA)</strong> instead of full multi-head — share KV heads across query heads to shrink the KV cache (the bottleneck at inference time).</li></ul><p>A single block, in PyTorch, is shorter than people expect:</p><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> torch<span style="color:#f92672">,</span> torch.nn<span style="color:#66d9ef">as</span> nn<span style="color:#f92672">,</span> torch.nn.functional<span style="color:#66d9ef">as</span> F</span></span><span style="display:flex;"><span/></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span><span style="color:#a6e22e">RMSNorm</span>(nn<span style="color:#f92672">.</span>Module):</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> __init__(self, dim, eps<span style="color:#f92672">=</span><span style="color:#ae81ff">1e-6</span>):</span></span><span style="display:flex;"><span> super()<span style="color:#f92672">.</span>__init__()</span></span><span style="display:flex;"><span> self<span style="color:#f92672">.</span>weight<span style="color:#f92672">=</span> nn<span style="color:#f92672">.</span>Parameter(torch<span style="color:#f92672">.</span>ones(dim))</span></span><span style="display:flex;"><span> self<span style="color:#f92672">.</span>eps<span style="color:#f92672">=</span> eps</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span><span style="color:#a6e22e">forward</span>(self, x):</span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span> self<span style="color:#f92672">.</span>weight<span style="color:#f92672">*</span> x<span style="color:#f92672">*</span> torch<span style="color:#f92672">.</span>rsqrt(x<span style="color:#f92672">.</span>pow(<span style="color:#ae81ff">2</span>)<span style="color:#f92672">.</span>mean(<span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>, keepdim<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>)<span style="color:#f92672">+</span> self<span style="color:#f92672">.</span>eps)</span></span><span style="display:flex;"><span/></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span><span style="color:#a6e22e">SwiGLU</span>(nn<span style="color:#f92672">.</span>Module):</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> __init__(self, dim, hidden):</span></span><span style="display:flex;"><span> super()<span style="color:#f92672">.</span>__init__()</span></span><span style="display:flex;"><span> self<span style="color:#f92672">.</span>w1<span style="color:#f92672">=</span> nn<span style="color:#f92672">.</span>Linear(dim, hidden, bias<span style="color:#f92672">=</span><span style="color:#66d9ef">False</span>)</span></span><span style="display:flex;"><span> self<span style="color:#f92672">.</span>w2<span style="color:#f92672">=</span> nn<span style="color:#f92672">.</span>Linear(hidden, dim, bias<span style="color:#f92672">=</span><span style="color:#66d9ef">False</span>)</span></span><span style="display:flex;"><span> self<span style="color:#f92672">.</span>w3<span style="color:#f92672">=</span> nn<span style="color:#f92672">.</span>Linear(dim, hidden, bias<span style="color:#f92672">=</span><span style="color:#66d9ef">False</span>)</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span><span style="color:#a6e22e">forward</span>(self, x):</span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span> self<span style="color:#f92672">.</span>w2(F<span style="color:#f92672">.</span>silu(self<span style="color:#f92672">.</span>w1(x))<span style="color:#f92672">*</span> self<span style="color:#f92672">.</span>w3(x))</span></span><span style="display:flex;"><span/></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span><span style="color:#a6e22e">Block</span>(nn<span style="color:#f92672">.</span>Module):</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> __init__(self, dim, n_heads, hidden):</span></span><span style="display:flex;"><span> super()<span style="color:#f92672">.</span>__init__()</span></span><span style="display:flex;"><span> self<span style="color:#f92672">.</span>norm1<span style="color:#f92672">=</span> RMSNorm(dim)</span></span><span style="display:flex;"><span> self<span style="color:#f92672">.</span>attn<span style="color:#f92672">=</span> nn<span style="color:#f92672">.</span>MultiheadAttention(dim, n_heads, bias<span style="color:#f92672">=</span><span style="color:#66d9ef">False</span>, batch_first<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>)</span></span><span style="display:flex;"><span> self<span style="color:#f92672">.</span>norm2<span style="color:#f92672">=</span> RMSNorm(dim)</span></span><span style="display:flex;"><span> self<span style="color:#f92672">.</span>ffn<span style="color:#f92672">=</span> SwiGLU(dim, hidden)</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span><span style="color:#a6e22e">forward</span>(self, x, mask):</span></span><span style="display:flex;"><span> h<span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>norm1(x)</span></span><span style="display:flex;"><span> a, _<span style="color:#f92672">=</span> self<span style="color:#f92672">.</span>attn(h, h, h, attn_mask<span style="color:#f92672">=</span>mask, need_weights<span style="color:#f92672">=</span><span style="color:#66d9ef">False</span>)</span></span><span style="display:flex;"><span> x<span style="color:#f92672">=</span> x<span style="color:#f92672">+</span> a</span></span><span style="display:flex;"><span> x<span style="color:#f92672">=</span> x<span style="color:#f92672">+</span> self<span style="color:#f92672">.</span>ffn(self<span style="color:#f92672">.</span>norm2(x))</span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span> x</span></span></code></pre></div><p>The full model is just<code>nn.Embedding</code> →<code>N × Block</code> →<code>RMSNorm</code> → tied output projection. Almost everything interesting about a 7B-vs-70B model comes from scale, data, and training procedure — not from architectural cleverness.</p><h2 id="pretraining">Pretraining</h2><p>Pretraining is conceptually simple — next-token prediction over the corpus — but the engineering is where the time goes. The loss is the standard cross-entropy</p><p>$$\mathcal{L}_{\text{CLM}}(\theta) = -\sum_{t=1}^{T} \log p_{\theta}(x_t \mid x_{&lt;t}),$$</p><p>averaged over a packed batch of sequences. A few choices that disproportionately matter:</p><ul><li><strong>Optimizer.</strong> AdamW with $\beta_1 = 0.9$, $\beta_2 = 0.95$ (note the lower $\beta_2$ — standard $0.999$ is too sluggish for LM pretraining), weight decay $0.1$, and gradient clipping at $1.0$.</li><li><strong>Learning-rate schedule.</strong> Linear warmup over the first ~1% of steps, then cosine decay to ~10% of peak. Peak LR scales roughly as $\sim 1/\sqrt{d_{\text{model}}}$.</li><li><strong>Batching.</strong> Pack multiple short documents into one fixed-length sequence, separated by<code>&lt;|endoftext|></code>, to keep GPU utilization high. Mask the loss across document boundaries if you care about clean per-doc semantics.</li><li><strong>Mixed precision.</strong> BF16 is now the default — same dynamic range as FP32, half the memory, no loss-scaling gymnastics.</li><li><strong>Distributed training.</strong> ZeRO-3 (DeepSpeed) or FSDP (PyTorch native) shards optimizer state, gradients, and parameters across GPUs. Tensor/pipeline parallelism enter once the model itself doesn&rsquo;t fit on one device.</li></ul><p>A skeleton training step, stripped down:</p><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> torch.utils.data<span style="color:#f92672">import</span> DataLoader</span></span><span style="display:flex;"><span/></span><span style="display:flex;"><span>opt<span style="color:#f92672">=</span> torch<span style="color:#f92672">.</span>optim<span style="color:#f92672">.</span>AdamW(model<span style="color:#f92672">.</span>parameters(), lr<span style="color:#f92672">=</span><span style="color:#ae81ff">3e-4</span>, betas<span style="color:#f92672">=</span>(<span style="color:#ae81ff">0.9</span>,<span style="color:#ae81ff">0.95</span>), weight_decay<span style="color:#f92672">=</span><span style="color:#ae81ff">0.1</span>)</span></span><span style="display:flex;"><span>sched<span style="color:#f92672">=</span> torch<span style="color:#f92672">.</span>optim<span style="color:#f92672">.</span>lr_scheduler<span style="color:#f92672">.</span>CosineAnnealingLR(opt, T_max<span style="color:#f92672">=</span>total_steps)</span></span><span style="display:flex;"><span/></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> step, batch<span style="color:#f92672">in</span> enumerate(DataLoader(packed_dataset, batch_size<span style="color:#f92672">=</span>bsz)):</span></span><span style="display:flex;"><span><span style="color:#66d9ef">with</span> torch<span style="color:#f92672">.</span>autocast(<span style="color:#e6db74">"cuda"</span>, dtype<span style="color:#f92672">=</span>torch<span style="color:#f92672">.</span>bfloat16):</span></span><span style="display:flex;"><span> logits<span style="color:#f92672">=</span> model(batch[<span style="color:#e6db74">"input_ids"</span>])</span></span><span style="display:flex;"><span> loss<span style="color:#f92672">=</span> F<span style="color:#f92672">.</span>cross_entropy(</span></span><span style="display:flex;"><span> logits[:, :<span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>]<span style="color:#f92672">.</span>reshape(<span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>, vocab),</span></span><span style="display:flex;"><span> batch[<span style="color:#e6db74">"input_ids"</span>][:,<span style="color:#ae81ff">1</span>:]<span style="color:#f92672">.</span>reshape(<span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>),</span></span><span style="display:flex;"><span> ignore_index<span style="color:#f92672">=</span>PAD_ID,</span></span><span style="display:flex;"><span> )</span></span><span style="display:flex;"><span> loss<span style="color:#f92672">.</span>backward()</span></span><span style="display:flex;"><span> torch<span style="color:#f92672">.</span>nn<span style="color:#f92672">.</span>utils<span style="color:#f92672">.</span>clip_grad_norm_(model<span style="color:#f92672">.</span>parameters(),<span style="color:#ae81ff">1.0</span>)</span></span><span style="display:flex;"><span> opt<span style="color:#f92672">.</span>step(); sched<span style="color:#f92672">.</span>step(); opt<span style="color:#f92672">.</span>zero_grad(set_to_none<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>)</span></span></code></pre></div><p>The single most important habit during pretraining is to<strong>plot the loss curve obsessively</strong> and watch for the failure modes you&rsquo;ve seen before: a sudden spike (lower the LR or skip the bad batch and resume), a slow drift upward (data ordering or numerical issue), a plateau that won&rsquo;t break (LR too low, or you&rsquo;ve actually saturated this dataset).</p><h2 id="supervised-fine-tuning">Supervised Fine-Tuning</h2><p>After pretraining you have a model that<em>completes</em> text. SFT is what turns it into a model that<em>responds</em> to text. The loss is still next-token prediction, but applied only to the assistant&rsquo;s reply — the prompt tokens are masked out so the model isn&rsquo;t penalized for &ldquo;predicting&rdquo; the user&rsquo;s message.</p><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span><span style="color:#a6e22e">sft_loss</span>(logits, labels, prompt_lens):</span></span><span style="display:flex;"><span><span style="color:#75715e"># mask out prompt tokens by setting their label to -100 (ignored by F.cross_entropy)</span></span></span><span style="display:flex;"><span> labels<span style="color:#f92672">=</span> labels<span style="color:#f92672">.</span>clone()</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> i, p<span style="color:#f92672">in</span> enumerate(prompt_lens):</span></span><span style="display:flex;"><span> labels[i, :p]<span style="color:#f92672">=</span><span style="color:#f92672">-</span><span style="color:#ae81ff">100</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span> F<span style="color:#f92672">.</span>cross_entropy(logits[:, :<span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>]<span style="color:#f92672">.</span>reshape(<span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>, vocab),</span></span><span style="display:flex;"><span> labels[:,<span style="color:#ae81ff">1</span>:]<span style="color:#f92672">.</span>reshape(<span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>),</span></span><span style="display:flex;"><span> ignore_index<span style="color:#f92672">=-</span><span style="color:#ae81ff">100</span>)</span></span></code></pre></div><p>Practical rules of thumb for SFT:</p><ul><li><strong>A few thousand to a few tens of thousands of examples is usually enough.</strong> More is not always better — once the model has learned the format, additional low-quality data hurts.</li><li><strong>Use a much smaller LR than pretraining</strong> (typically $1e\text{-}5$ to $5e\text{-}5$) and a<em>very</em> short warmup. SFT moves the model in distribution-space; aggressive updates erase pretraining knowledge (&ldquo;catastrophic forgetting&rdquo;).</li><li><strong>Train for 1–3 epochs.</strong> Beyond that the model starts to memorize the SFT set and loses generality.</li><li><strong>Always keep a held-out eval set drawn from the<em>same distribution as your real users</em></strong> — public benchmarks are useful but rarely predict deployment behavior.</li></ul><h2 id="preference-alignment-from-rlhf-to-dpo">Preference Alignment: from RLHF to DPO</h2><p>SFT teaches the model<em>one way</em> to respond. Preference alignment teaches it<em>which of two responses is better</em>, which is a much weaker but much cheaper supervision signal — humans are far better at picking the preferred answer than at writing the ideal one.</p><p>Classical<strong>RLHF</strong> [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-5" title="L. Ouyang, J. Wu, X. Jiang, D. Almeida, C. Wainwright, P. Mishkin, C. Zhang, S. Agarwal, K. Slama, A. Ray, and others, Training language models to follow instructions with human feedback, In Proc. Advances in Neural Information Processing Systems, 2022">5</a>] is the three-step pipeline already sketched in the task list: (1) SFT, (2) train a reward model $r_{\phi}(x, y)$ on preference pairs $(x, y_w \succ y_l)$ with the Bradley–Terry loss
$$\mathcal{L}_{\text{RM}}(\phi) = -\mathbb{E}_{(x, y_w, y_l)}\left[\log \sigma\bigl(r_{\phi}(x, y_w) - r_{\phi}(x, y_l)\bigr)\right],$$
and (3) fine-tune the policy $\pi_{\theta}$ with PPO against $r_{\phi}$ while penalizing KL drift from the SFT reference $\pi_{\text{ref}}$. It works, but it is finicky: PPO is unstable, the reward model can be hacked, and you end up serving four models at training time (policy, reference, reward, value).</p><p><strong>Direct Preference Optimization (DPO)</strong> [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-6" title="R. Rafailov, A. Sharma, E. Mitchell, S. Ermon, C. Manning, and C. Finn, Direct preference optimization: Your language model is secretly a reward model, In Proc. Advances in Neural Information Processing Systems, 2023">6</a>] is the recipe most teams now reach for first. The insight is algebraic: under the standard KL-regularized RL objective, the optimal policy has a closed-form relationship to the reward model, which means you can re-parameterize away the reward model entirely and optimize the policy directly on preference pairs:
$$\mathcal{L}_{\text{DPO}}(\theta) = -\mathbb{E}_{(x, y_w, y_l)}!\left[\log \sigma!\left(\beta \log\frac{\pi_{\theta}(y_w \mid x)}{\pi_{\text{ref}}(y_w \mid x)} - \beta \log\frac{\pi_{\theta}(y_l \mid x)}{\pi_{\text{ref}}(y_l \mid x)}\right)\right].$$</p><p>No reward model, no rollouts, no PPO — just two forward passes (policy and frozen reference) on the same preference pair. The single hyperparameter $\beta$ controls how strongly the policy is allowed to drift from $\pi_{\text{ref}}$ (smaller $\beta$ = more aggressive). DPO is not a strict superset of RLHF — it can&rsquo;t easily incorporate online exploration or non-pairwise rewards — but for the common case of &ldquo;I have a preference dataset, make my model better&rdquo; it is dramatically simpler and usually competitive.</p><p>A handful of DPO variants are worth knowing about:<strong>IPO</strong> (avoids overfitting when preferences are deterministic),<strong>KTO</strong> (works with binary thumbs-up/down instead of pairs),<strong>ORPO</strong> (folds SFT and preference loss into one stage so you don&rsquo;t need a separate reference model), and<strong>SimPO</strong> (reference-free, length-normalized). They mostly differ in how they handle the reference policy and how they normalize for response length.</p><h2 id="evaluation">Evaluation</h2><p>Loss curves go down. Benchmarks go up. Neither tells you whether the model is actually useful. A reasonable evaluation stack has three layers:</p><ol><li><strong>Automatic, broad-coverage benchmarks</strong> — MMLU, GSM8K, HumanEval, IFEval, MT-Bench. Cheap, reproducible, and easy to over-fit to. Track them, but never<em>only</em> them.</li><li><strong>Task-specific evals you wrote yourself</strong> — a few hundred prompts that look like your actual product traffic, scored either by a stronger LLM judge or by a rubric. This is where regressions actually show up.</li><li><strong>Human evaluation on a small, representative slice.</strong> Slow and expensive, but the only signal that captures what users will actually feel. Use it to<em>calibrate</em> your LLM-judge scores, not to replace them.</li></ol><p>For pretraining specifically, perplexity on a held-out shard of the same distribution is the cleanest signal of &ldquo;is the model still learning?&rdquo; Cross-distribution perplexity (e.g., perplexity on code when training on web) is a useful early-warning for over-specialization.</p><h2 id="closing-thoughts">Closing Thoughts</h2><p>Building a language model from scratch is less a single algorithm and more a stack of decisions, each of which is individually boring but collectively determines whether the final model is any good. The dominant lesson of the last five years is that the<em>boring</em> parts — data quality, tokenization, evaluation discipline — usually matter more than the exciting parts (architecture tweaks, novel objectives). The architectural recipe converged to a small handful of choices; the post-training recipe is converging on DPO-family methods; what&rsquo;s left to differentiate models is mostly data and care.</p><p>If you&rsquo;re doing this for the first time, my suggestion is to start small — a 100M-parameter model on a few billion tokens — and run the<em>entire</em> pipeline end-to-end (clean → tokenize → pretrain → SFT → DPO → eval) before scaling any single stage. The bugs you find at small scale are the same bugs you would have found at large scale, except they cost you a weekend instead of a quarter.</p><h2 id="reference">Reference</h2><ol class="hugo-simplecite-reference-list"><li class="hugo-simplecite-reference-list-item" id="bibreference-1">J. Devlin, M. Chang, K. Lee, and K. Toutanova,<q>Bert: Pre-training of deep bidirectional transformers for language understanding,</q> In Proc. Proceedings of the 2019 conference of the north american chapter of the association for computational linguistics: Human language technologies, volume 1 (long and short papers), 2019, pp. 4171–4186.</li><li class="hugo-simplecite-reference-list-item" id="bibreference-2">H. Touvron, L. Martin, K. Stone, and others,<q>Llama 2: Open foundation and fine-tuned chat models,</q><em>arXiv preprint arXiv:2307.09288</em>, 2023.</li><li class="hugo-simplecite-reference-list-item" id="bibreference-3">R. Sennrich, B. Haddow, and A. Birch,<q>Neural machine translation of rare words with subword units,</q> In Proc. Proceedings of the 54th Annual Meeting of the Association for Computational Linguistics, 2016, pp. 1715–1725.</li><li class="hugo-simplecite-reference-list-item" id="bibreference-4">A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. Gomez, Ł. Kaiser, and I. Polosukhin,<q>Attention is all you need,</q> In Proc. Advances in Neural Information Processing Systems, 2017</li><li class="hugo-simplecite-reference-list-item" id="bibreference-5">L. Ouyang, J. Wu, X. Jiang, D. Almeida, C. Wainwright, P. Mishkin, C. Zhang, S. Agarwal, K. Slama, A. Ray, and others,<q>Training language models to follow instructions with human feedback,</q> In Proc. Advances in Neural Information Processing Systems, 2022</li><li class="hugo-simplecite-reference-list-item" id="bibreference-6">R. Rafailov, A. Sharma, E. Mitchell, S. Ermon, C. Manning, and C. Finn,<q>Direct preference optimization: Your language model is secretly a reward model,</q> In Proc. Advances in Neural Information Processing Systems, 2023</li></ol>
]]></content:encoded></item></channel></rss>