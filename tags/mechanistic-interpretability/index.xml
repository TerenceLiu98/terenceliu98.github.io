<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>mechanistic interpretability | 特倫蘇的日與夜</title><link>https://blog.cklau.cc/tags/mechanistic-interpretability/</link><atom:link href="https://blog.cklau.cc/tags/mechanistic-interpretability/index.xml" rel="self" type="application/rss+xml"/><description>mechanistic interpretability</description><generator>Source Themes Academic (https://sourcethemes.com/academic/)</generator><language>en</language><lastBuildDate>Thu, 23 Apr 2026 00:18:00 +0000</lastBuildDate><item><title>🧠 Four Eras of LLM Mechanistic Interpretability — and the Axis Nobody is Watching</title><link>https://blog.cklau.cc/scientia/llm-mechanistic-interpretability/</link><pubDate>Thu, 23 Apr 2026 00:18:00 +0000</pubDate><author>Junjie LIU</author><category>Artificial Intelligence</category><category>XAI</category><category>interpretability</category><category>mechanistic interpretability</category><category>LLM</category><guid>https://blog.cklau.cc/scientia/llm-mechanistic-interpretability/</guid><description>TL;DR Mechanistic interpretability of transformers has matured along three axes: states (Logit / Tuned Lens, probing), causality (activation patching, ACDC, feature circuits), and contrastive states (RepE, CAA, steering vectors). Most interpretability blogs cover the first three. A fourth lineage has been quietly developing in the dynamical-systems community — block-Jacobian spectra, mean-field/Vlasov limits, phase portraits, Langevin/Fokker–Planck fits, Koopman/DMD, Liang–Kleeman information flow — but it studies the residual stream unconditionally, as background, not under task contrasts.</description><content:encoded>&lt;![CDATA[<h2 id="tldr">TL;DR</h2><ul><li>Mechanistic interpretability of transformers has matured along<strong>three axes</strong>:<em>states</em> (Logit / Tuned Lens, probing),<em>causality</em> (activation patching, ACDC, feature circuits), and<em>contrastive states</em> (RepE, CAA, steering vectors). Most interpretability blogs cover the first three.</li><li>A<strong>fourth lineage</strong> has been quietly developing in the dynamical-systems community — block-Jacobian spectra, mean-field/Vlasov limits, phase portraits, Langevin/Fokker–Planck fits, Koopman/DMD, Liang–Kleeman information flow — but it studies the residual stream<strong>unconditionally</strong>, as background, not under task contrasts.</li><li>The<strong>missing fourth axis</strong> is<em>differential transition operators</em>: ask not &ldquo;what state does the model represent&rdquo; or &ldquo;which components fire&rdquo;, but &ldquo;how does the layer-$\ell$ update map $f_\ell$ change between two task conditions?&rdquo; Object from Axis 4, framing from Axes 2/3 — nobody has explicitly bridged them.</li><li>The cross-cutting issue: there is<strong>no unified pathway-share metric</strong> (&ldquo;how much of the work at this layer goes through attention vs MLP&rdquo;) that is comparable across tasks and across model scales. That metric naturally lives on the<em>transition</em>, not the<em>state</em> — which is another way of saying the missing axis is missing for an operational reason, not a taxonomical one.</li><li><strong>Audience:</strong> someone who has read the IOI paper and Logit Lens, but not the dynamics literature. The dynamics section is where this post adds the most value.</li></ul><h2 id="before">Before</h2><p>Most interpretability blog posts walk the reader through a list of methods in roughly chronological order — Logit Lens first, then activation patching, then ACDC, then sparse autoencoders, then steering vectors. After reading them you know what each method is<em>called</em>, but you don&rsquo;t really know<em>what question each method commits to answering</em>. The literature is small enough that the methods recombine, and the chronological ordering flattens what is actually going on.</p><p>I think a much more useful organization is by<strong>target</strong>: which aspect of the model does each generation of methods promise to explain? Once you read the field this way, the existing eras line up onto three axes —<strong>states, causality, dynamics</strong> — and a fourth axis,<strong>differential transition operators</strong>, is sitting in plain sight, mostly because the people who would notice it (the interpretability crowd) and the people who already have the right tools (dynamical systems, optimal transport, information geometry) do not really read each other&rsquo;s papers.</p><p>This post is for someone who has read the IOI paper [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-1" title="K. Wang, A. Variengien, A. Conmy, B. Shlegeris, and J. Steinhardt, Interpretability in the wild: A circuit for indirect object identification in GPT-2 small, In Proc. International Conference on Learning Representations, 2023">1</a>] and skimmed Logit Lens, but has never heard of Koopman operators or Fokker–Planck. The first three axes are a fast recap. The fourth one is where the most interesting unread literature lives, and where I think the next wave of mechanistic insight is going to come from.</p><h2 id="axis-1--states-what-is-linearly-there-and-at-what-depth">Axis 1 — States: what is linearly there, and at what depth?</h2><p>The oldest interpretability tradition for transformers is<strong>state readout</strong>. Take the hidden activation at some intermediate layer $\ell$, project it back into vocabulary space, and read off what the model &ldquo;thinks&rdquo; it is going to say at that depth. nostalgebraist&rsquo;s<a href="https://www.lesswrong.com/posts/AcKRB8wDpdaN6v6ru/interpreting-gpt-the-logit-lens">Logit Lens post on LessWrong (2020)</a> is the canonical version: apply the unembedding matrix $W_U$ directly to the residual stream $h_\ell$, and look at the top-k tokens of $\text{softmax}(W_U h_\ell)$. The result is surprisingly informative — by mid-depth the residual stream&rsquo;s top tokens already concentrate on plausible continuations, which is itself a non-trivial fact about how the model builds answers.</p><p>The trouble is that Logit Lens implicitly assumes the basis at every layer is<em>the same</em> basis the unembedding was trained on. That is only approximately true.<strong>Tuned Lens</strong> [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-2" title="N. Belrose, Z. Furman, L. Smith, D. Halawi, I. Ostrovsky, L. McKinney, S. Biderman, and J. Steinhardt, Eliciting latent predictions from transformers with the tuned lens, arXiv preprint arXiv:2303.08112, 2023. ">2</a>] fixes this with a small affine probe $A_\ell$ trained per layer so that $\text{softmax}(W_U A_\ell h_\ell)$ matches the final-layer distribution; the lens becomes calibrated rather than naive. Linear probing is the same family of idea pushed further: train a small classifier on top of $h_\ell$ to recover any property you care about — sentiment, syntax, factual recall, position, the answer to a multiple-choice question — and ask at which depth the property becomes linearly decodable.</p><p>Together these methods answer one question well:<strong>what does the model represent, and where in the stack?</strong> They are silent on a second, harder question:<strong>how is that representation built?</strong> A linear probe at layer 12 tells you that subject information is there; it does not tell you which earlier layers wrote it, what computation produced it, or how stable the encoding is under perturbation.</p><h2 id="axis-2--causality-which-components-are-necessary">Axis 2 — Causality: which components are necessary?</h2><p>The intervention era is the response to that gap. The unifying move is<strong>counterfactual ablation</strong>: run the model on a clean prompt $x$, run it again on a corrupted prompt $x&rsquo;$ that differs in exactly one feature, then<em>patch</em> some component (a head&rsquo;s output, an MLP block, a single residual position) from one run into the other and see if the prediction follows the patch. If patching head $\ell.h$ from the clean run into the corrupted run recovers the clean prediction, that head is<em>causally responsible</em> for the behavior under study.</p><p>Activation patching gives you per-component effect sizes.<strong>Attribution patching</strong> linearizes the patch using a first-order gradient approximation, so that you do not need to actually rerun the model once per intervention — a huge speedup that turns a quadratic-cost search into a single backward pass.<strong>ACDC</strong> (Automated Circuit DisCovery) [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-3" title="A. Conmy, A. Mavor-Parker, A. Lynch, S. Heimersheim, and A. Garriga-Alonso, Towards automated circuit discovery for mechanistic interpretability, In Proc. Advances in Neural Information Processing Systems, 2023">3</a>] automates the obvious next step: greedily prune the model&rsquo;s computation graph until only the components whose removal hurts the metric remain, and report the resulting sparse subgraph as a &ldquo;circuit.&rdquo; Feature circuits push the same idea down to SAE features rather than whole heads or MLPs.</p><p>The intervention era is what lets us actually make claims like<em>&ldquo;this head copies the indirect object&rdquo;</em> or<em>&ldquo;this MLP stores the capital-of-country fact.&rdquo;</em> It is the most causally honest axis we have. The cost is twofold: (a) every claim is per-task — one circuit per behavior, and the search is expensive per hypothesis; and (b) the output is a<em>graph of components</em>, not a description of the<em>transition</em> connecting them. Patching tells you head 9.6 matters for IOI; it does not give you an operator that maps &ldquo;subject is in residual stream&rdquo; to &ldquo;indirect object will be next token.&rdquo;</p><h2 id="axis-3--contrastive-representations-shifts-as-the-manipulation-handle">Axis 3 — Contrastive representations: shifts as the manipulation handle</h2><p>The third axis takes a more pragmatic stance. Instead of asking which components compute a behavior, it asks:<strong>does the activation difference between behaviors live in a low-dimensional subspace, and can I move along that subspace to control the behavior?</strong></p><p>The recipe is the same across the whole literature. Collect activations on contrastive prompt pairs —<em>&ldquo;happy&rdquo; vs &ldquo;sad&rdquo;, &ldquo;truthful&rdquo; vs &ldquo;deceptive&rdquo;, &ldquo;refuses&rdquo; vs &ldquo;complies&rdquo;</em> — take the mean activation difference $\Delta h_\ell = \mathbb{E}[h_\ell \mid +] - \mathbb{E}[h_\ell \mid -]$, and use that vector as both an<strong>analysis object</strong> (a direction in activation space tagged with semantic meaning) and a<strong>manipulation handle</strong> (add or subtract a scaled copy at inference time and watch the behavior change).<strong>Representation Engineering</strong> [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-4" title="A. Zou, L. Phan, S. Chen, J. Campbell, P. Guo, R. Ren, A. Pan, X. Yin, M. Mazeika, A. Dombrowski, and others, Representation engineering: A top-down approach to AI transparency, arXiv preprint arXiv:2310.01405, 2023. ">4</a>]<span class="sidenote-number"><small class="sidenote">
A bit of a name collision with "representation learning" — they are not the same thing, despite the vocabulary overlap.</small></span> formalizes this for high-level concepts like honesty and emotion;<strong>Contrastive Activation Addition (CAA)</strong> is the same idea localized to a single layer; the broader<strong>steering-vector</strong> literature treats it as a general control surface for safety-relevant behaviors.</p><p>This axis is appealing because it links<strong>state geometry to behavior</strong> with one cheap forward pass. But the limitation is built into the construction: it operates at the<em>state</em> level, not at the<em>transition</em> level. A steering vector tells you which direction in $\mathbb{R}^{d}$ correlates with the contrast; it does not tell you how the<em>update map</em> differs between the two conditions. If two behaviors share the same mean shift but differ in<em>how</em> the residual stream evolves around that shift — different curvature, different mixing rates, different attention routing — the steering vector simply cannot see it.</p><p>So Axes 1 and 3 really both belong to the same axis of interest: they target<strong>state</strong>. Axis 1 asks what is decodable from the state, Axis 3 asks what direction in state-space shifts behavior. Axis 2 is the<strong>causality</strong> axis. None of the three speak to<em>how the state moves</em>.</p><h2 id="axis-4--dynamics-the-literature-interpretability-never-reads">Axis 4 — Dynamics: the literature interpretability never reads</h2><p>This is where the post starts to earn its keep, because almost no interpretability blog mentions this lineage at all. The residual stream is, after all, a<strong>trajectory</strong>:</p><p>$$h_{0} \to h_{1} \to \dots \to h_{L}, \quad h_{\ell+1} = h_{\ell} + f_{\ell}(h_{\ell}),$$</p><p>where $f_{\ell}$ is whatever the $\ell$-th block computes (attention + MLP, with a residual bypass). That is a discrete-time dynamical system, and dynamical-systems people have a<em>lot</em> to say about discrete-time dynamical systems. There are at least five threads worth knowing about, none of them well-known to interpretability readers:</p><ul><li><p><strong>Block-Jacobian spectra.</strong> Li et al. [<a class="hugo-simplecite-cite-hyperlink" href="#bibreference-5" title="J. Li and V. Papyan, Residual alignment: uncovering the mechanisms of residual networks, In Proc. Advances in Neural Information Processing Systems, 2023, vol. 36, pp. 57660–57712. ">5</a>] compute the Jacobian of one transformer block, $J_\ell = \partial h_{\ell+1}/\partial h_\ell$, and characterize its eigenvalue distribution across depth — what they call<em>residual alignment</em>: the residual updates align with a shrinking set of singular directions as depth grows. The spectrum shifts in a structured way: early layers behave like contraction, mid layers expand a few subspaces, late layers re-collapse onto a low-dimensional output manifold. This already gives you a depth-dependent<strong>operator</strong> picture rather than a state picture.</p></li><li><p><strong>Mean-field / Vlasov limits.</strong> Castin et al. take the infinite-token limit of self-attention and derive a continuity equation for the<em>empirical distribution</em> of token representations, treating the residual stream as a flow on a measure space. The transformer becomes a discretization of a particular partial differential equation. This is the cleanest theoretical entry point for asking &ldquo;what does attention actually<em>do</em> to a distribution of tokens?&rdquo;</p></li><li><p><strong>Phase portraits.</strong> Fernando and Guitchounts visualize forward dynamics as trajectories in low-dimensional projections of the residual stream, looking for fixed points, limit cycles, and saddle structure. The picture that emerges — depth as a slow flow toward attractor manifolds — is qualitatively different from the &ldquo;the model thinks at layer 12&rdquo; mental image you get from probing.</p></li><li><p><strong>Stochastic / Fokker–Planck fits.</strong> Sarfati et al. fit a Langevin-type model
$$dh = -\nabla U(h),d\ell + \sigma,dW_\ell$$
to the residual-stream evolution, recovering an effective potential $U(h)$ whose minima correspond to high-confidence answers. The picture this gives you, if you have not seen it before, is genuinely useful:<strong>forward depth is integration time</strong> (each layer is one step of an SDE rather than an arbitrary computation), the residual stream<strong>flows downhill on a potential landscape</strong> $U(h)$, and the equilibrium distribution at any depth has the Boltzmann form $p(h) \propto e^{-U(h)/T}$ — so the model&rsquo;s softmax confidence is reinterpreted as a<em>temperature on the basins of $U$</em>. Equivalently, the time-evolution of the<em>distribution</em> over residual streams obeys the Fokker–Planck equation
$$\partial_\ell,p(h, \ell) = \nabla \cdot \bigl(p,\nabla U\bigr) + \tfrac{\sigma^{2}}{2}\nabla^{2} p,$$
which is the dual, distributional view that Vlasov / mean-field analyses operate in. This is the only thread in the lineage that explicitly treats depth as integration time, and it brings free-energy and potential-landscape vocabulary along with it — which is the kind of vocabulary interpretability is going to need once it starts asking<em>transition</em>-level questions.</p></li><li><p><strong>Operator-theoretic methods (Koopman, DMD).</strong> Approaches in the spirit of ATO apply Dynamic Mode Decomposition to the layer-by-layer activations to extract a<em>linear</em> operator that best explains the (nonlinear) state evolution. The Koopman framing is powerful precisely because it converts a nonlinear dynamical question into a linear<em>spectral</em> one — modes, frequencies, decay rates — which is exactly the structure that interpretability is starving for.</p></li><li><p><strong>Information-theoretic flow (Liang–Kleeman).</strong> The Liang–Kleeman framework quantifies the<em>causal influence</em> of one variable on another in a dynamical system, with units of bits per time step, and importantly without conflating correlation with causation. Applied to the residual stream, it would say not just &ldquo;head 9.6 matters&rdquo; but &ldquo;head 9.6 transmits $X$ bits per layer to position $i$ about feature $Y$.&rdquo; Almost nobody in the interpretability literature has actually done this at scale, which is itself notable.</p></li></ul><p>The crucial caveat — and this is the thing most interpretability readers miss when they finally do read these papers — is that<strong>this entire lineage studies<em>unconditional</em> dynamics</strong>. It characterizes the flow on average, or in expectation over the data distribution, or as a property of the trained network independent of any task. It is<strong>background structure</strong>, not task-specific structure. The Jacobian spectrum of layer 9 is a property of the network; the IOI circuit is a property of the network<em>under a particular task contrast</em>. These are different objects, and so far they live in different papers, written by different communities.</p><h2 id="the-gap--differential-transition-operators">The Gap — Differential Transition Operators</h2><p>Put the four eras side by side:</p><table><thead><tr><th>Axis</th><th>Object of study</th><th>Granularity</th><th>Conditional on task?</th></tr></thead><tbody><tr><td>1. States (Logit / Tuned Lens, probes)</td><td>$h_\ell$</td><td>per layer</td><td>partly — probes are</td></tr><tr><td>2. Causality (patching, ACDC, feature circuits)</td><td>components</td><td>per head/MLP</td><td>yes — one circuit per task</td></tr><tr><td>3. Contrastive states (RepE, CAA, steering)</td><td>$\Delta h_\ell$</td><td>per layer/region</td><td>yes</td></tr><tr><td>4. Dynamics (Jacobians, Koopman, Vlasov, L-K)</td><td>transition $f_\ell$</td><td>per layer</td><td><strong>no — unconditional</strong></td></tr></tbody></table><p>The diagonal entry is empty. There is no body of work that asks<em>&ldquo;how does the transition operator $f_\ell$ change between two task conditions?&rdquo;</em> Axis 4 has the right object (the<em>transition</em>); Axes 2 and 3 have the right framing (<em>contrastive, conditional</em>). Nobody has explicitly put them together.</p><p>That combination — call it<strong>differential transition operators</strong> — would let you say things like:</p><blockquote><p>&ldquo;Under the IOI task vs. its negative control, the layer-9 transition operator gains a rank-2 component that routes from position 7 to the residual at position 11, with information flow of $X$ bits per layer.&rdquo;</p></blockquote><p>That is a statement about<em>how the computation differs</em> between two conditions, not about<em>which components fire</em> (Axis 2) or<em>what direction the state shifts</em> (Axis 3) or<em>how the network flows on average</em> (Axis 4). It is a fourth, genuinely new axis.</p><p>The striking thing is how much of the existing toolbox already supports this. Block-Jacobian spectra trivially differ across conditions; you just have to compute them under a contrast pair. Koopman/DMD on residual streams admit a natural decomposition into<em>condition-specific</em> modes. Liang–Kleeman is<em>defined</em> contrastively. The ingredients are sitting on the shelf. Almost nobody is mixing them.</p><h2 id="a-cross-cutting-sidebar-attention-vs-mlp-and-why-scale-changes-the-answer">A Cross-cutting Sidebar: Attention vs MLP, and Why Scale Changes the Answer</h2><p>A natural objection at this point is:<em>&ldquo;but the IOI circuit is almost entirely attention heads, so why bother with transition operators when components already explain the behavior?&rdquo;</em></p><p>The reply is that IOI is GPT-2 small — 117M parameters, 12 layers, with a task picked specifically because it has a clean attention-routing structure. The folklore that</p><blockquote><p><strong>attention = routing/copying/induction,</strong> and<strong>MLP = key–value memory</strong></p></blockquote><p>is a useful heuristic at that scale, but it starts to break down well before you reach 1B parameters: gated-FFN and MoE architectures push MLPs into routing-like behavior, attention picks up more pure memorization (especially in long-context retrieval), and the cleanly separable &ldquo;pathway-share&rdquo; you can compute on GPT-2 small becomes much fuzzier as the model grows.</p><p>What we do<em>not</em> have today is a unified pathway-share metric — one number per layer, comparable across tasks and across model scales, that says<em>&ldquo;of the work done at this layer, $\alpha$ went through attention and $1-\alpha$ through the MLP.&rdquo;</em> That metric is naturally a<strong>transition</strong> quantity, not a<strong>state</strong> quantity: it lives on $f_\ell$, not on $h_\ell$. Which is another way of saying that the missing fourth axis is missing for a concrete and operational reason, not just a taxonomical one.</p><h2 id="closing">Closing</h2><p>If you have spent your time reading the canonical interpretability blogs — Logit Lens, IOI, ACDC, the SAE papers, the steering-vector posts — your mental model of the field is probably organized by<em>method</em>. I think the more useful organization is by<em>what the method commits to explaining</em>: states, causality, contrastive states, or transitions. The first three are mature. The fourth — dynamics — has a deep, careful, mostly-unread literature attached to it, but that literature studies the network<em>unconditionally</em>, which is exactly the thing that interpretability does not need.</p><p>If I had to place one bet on what the next genuinely interesting interpretability tool will look like, it would be a Koopman-style (or Jacobian-spectrum, or Liang–Kleeman) analysis run<strong>contrastively</strong> across two task conditions, and reported as a per-layer, per-pathway<em>differential</em> operator. The math is sitting on the shelf. The question is whether anyone will pick it up before the next round of &ldquo;we patched some heads on a new task&rdquo; papers.</p><h2 id="further-reading-community-posts">Further Reading (Community Posts)</h2><p>A lot of the most useful mechanistic-interpretability material lives outside conventional venues — LessWrong, the Alignment Forum, Anthropic&rsquo;s transformer-circuits.pub, Neel Nanda&rsquo;s blog. Some posts I keep coming back to:</p><ul><li><strong>nostalgebraist (2020),<a href="https://www.lesswrong.com/posts/AcKRB8wDpdaN6v6ru/interpreting-gpt-the-logit-lens"><em>Interpreting GPT: the Logit Lens</em></a>.</strong> The original Logit Lens post. Read this first if you have not — most of Axis 1 is a refinement of the empirical observation made here.</li><li><strong>Anonymous,<a href="https://www.lesswrong.com/posts/ATsvzF77ZsfWzyTak/dataset-sensitivity-in-feature-matching-and-a-hypothesis-on-1"><em>SAE Training Dataset Influence in Feature Matching and a Hypothesis on Position Features</em></a>.</strong> A nice reminder that<em>which dataset you train your SAE on</em> changes which features it discovers — i.e. SAE features are not a property of the model alone, but of the (model, dataset) pair. Worth reading because it shows that even &ldquo;model-internal&rdquo; objects are silently conditioned on data, which is exactly the contrast/conditioning move I am pushing for in Axis 4.</li><li><strong>Elhage et al.,<a href="https://transformer-circuits.pub/2021/framework/index.html"><em>A Mathematical Framework for Transformer Circuits</em></a> (Anthropic, 2021).</strong> The bilinear / rank decomposition that makes the QK-OV picture of attention legible; the conceptual scaffolding most subsequent circuit papers borrow from.</li><li><strong>Olsson et al.,<a href="https://transformer-circuits.pub/2022/in-context-learning-and-induction-heads/index.html"><em>In-context Learning and Induction Heads</em></a> (Anthropic, 2022).</strong> The induction-head story; useful as a worked example of &ldquo;we found the mechanism&rdquo; at small scale, and a useful baseline for asking when that picture stops working at larger scale.</li><li><strong>Neel Nanda,<a href="https://www.neelnanda.io/mechanistic-interpretability/glossary"><em>A Comprehensive Mechanistic Interpretability Explainer &amp; Glossary</em></a>.</strong> Probably the single best entry point if you are landing in this field cold.</li><li><strong>Neel Nanda,<a href="https://www.lesswrong.com/posts/jP9KDyMkchuv6tHwm/how-to-become-a-mechanistic-interpretability-researcher"><em>How To Become A Mechanistic Interpretability Researcher</em></a> (2025).</strong> A staged curriculum and career guide rather than a technical post. If you are reading this blog because you want to<em>do</em> mech-interp, not just understand the literature, this is the practical companion piece — what to learn, in what order, how to find mentorship, how to ship work.</li></ul><p>If you only have time for one of these, read nostalgebraist&rsquo;s Logit Lens post — almost everything in Axis 1 is downstream of it.</p><h2 id="reference">Reference</h2><ol class="hugo-simplecite-reference-list"><li class="hugo-simplecite-reference-list-item" id="bibreference-1">K. Wang, A. Variengien, A. Conmy, B. Shlegeris, and J. Steinhardt,<q>Interpretability in the wild: A circuit for indirect object identification in GPT-2 small,</q> In Proc. International Conference on Learning Representations, 2023</li><li class="hugo-simplecite-reference-list-item" id="bibreference-2">N. Belrose, Z. Furman, L. Smith, D. Halawi, I. Ostrovsky, L. McKinney, S. Biderman, and J. Steinhardt,<q>Eliciting latent predictions from transformers with the tuned lens,</q><em>arXiv preprint arXiv:2303.08112</em>, 2023.</li><li class="hugo-simplecite-reference-list-item" id="bibreference-3">A. Conmy, A. Mavor-Parker, A. Lynch, S. Heimersheim, and A. Garriga-Alonso,<q>Towards automated circuit discovery for mechanistic interpretability,</q> In Proc. Advances in Neural Information Processing Systems, 2023</li><li class="hugo-simplecite-reference-list-item" id="bibreference-4">A. Zou, L. Phan, S. Chen, J. Campbell, P. Guo, R. Ren, A. Pan, X. Yin, M. Mazeika, A. Dombrowski, and others,<q>Representation engineering: A top-down approach to AI transparency,</q><em>arXiv preprint arXiv:2310.01405</em>, 2023.</li><li class="hugo-simplecite-reference-list-item" id="bibreference-5">J. Li and V. Papyan,<q>Residual alignment: uncovering the mechanisms of residual networks,</q> In Proc. Advances in Neural Information Processing Systems, 2023, vol. 36, pp. 57660–57712.</li></ol>
]]></content:encoded></item></channel></rss>